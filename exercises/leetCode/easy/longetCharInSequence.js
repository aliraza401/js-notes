const longestCharInSequence = (str) => {
  if (typeof str !== "string") return new Error("Invalid Input");

  const strMap = {};
  const counter = {};

  for(let index = 0; index < str.length; index++) {
    
  }

  return str;
};

console.log(longestCharInSequence("aaBBkccc"));
